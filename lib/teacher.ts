/* export const isTeacher = (userId?: string | null) => {
  return userId === process.env.NEXT_PUBLIC_TEACHER_ID;
} */

  //this makes the teacher to need to have an id to be able to access the teacher page